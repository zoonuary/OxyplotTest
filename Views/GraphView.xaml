<UserControl x:Class="OxyTest.Views.GraphView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:OxyTest.Views"
			 xmlns:dxmvvm="http://schemas.devexpress.com/winfx/2008/xaml/mvvm" xmlns:ViewModels="clr-namespace:OxyTest.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="30"/>
			<RowDefinition Height="*"/>
		</Grid.RowDefinitions>
		<Grid>
			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="*"/>
				<ColumnDefinition Width="*"/>
				<ColumnDefinition Width="*"/>
			</Grid.ColumnDefinitions>
			<Button Content="start" Command="{Binding CMD_START}" Grid.Column="0"/>
			<Button Content="stop" Command="{Binding CMD_STOP}" Grid.Column="1"/>
			<Button Content="append graph" Command="{Binding CMD_AppendGraph}" Grid.Column="2"/>
		</Grid>

		<Grid Grid.Row="1">
			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="*"/>
				<ColumnDefinition Width="*"/>
			</Grid.ColumnDefinitions>
			<local:GridView DataContext="{Binding GridViewModel}"/>
			<!--<local:GridView DataContext="{Binding GridViewModel}"/>-->
			<Grid x:Name="GraphPlotArea" Grid.Column="1"/>
		</Grid>
		
	</Grid>
</UserControl>
